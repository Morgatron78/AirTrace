export const APP_NAME = 'AirTrace'

// Sourced from package.json's own `version` field at build time via
// vite.config.js's `define` — see CLAUDE.md's Versioning section for why
// (one number to bump per release instead of several kept in sync by hand).
export const APP_VERSION = __APP_VERSION__
